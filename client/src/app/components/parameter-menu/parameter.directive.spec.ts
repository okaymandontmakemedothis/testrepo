import { ParameterDirective } from './parameter.directive';

describe('ParameterDirective', () => {
  it('should create an instance', () => {
    const directive = new ParameterDirective();
    expect(directive).toBeTruthy();
  });
});
